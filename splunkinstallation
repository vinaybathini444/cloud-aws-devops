Docker installation in EC2 
	 sudo yum update -y
	 sudo amazon-linux-extra install docker/ yum install docker
	 sudo amazon-linux-extra install docker
	 sudo usermod -a -G docker ec2-user
	 docker info

Docker Compose installation in EC2 
1. Run this command to download the current stable release of Docker Compose:
curl -SL https://github.com/docker/compose/releases/download/v2.5.0/docker-compose-linux-x86_64 -o /usr/local/bin/docker-compose

2. Apply executable permissions to the binary:
$ sudo chmod +x /usr/local/bin/docker-compose

3. Test the installation:
$ docker-compose --version


https://github.com/immontilla/splunk/tree/master



